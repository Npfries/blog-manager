/**
 * @typedef {import("fastify/types/instance").FastifyInstance} Fastify
 * @typedef {{blogpostService: import('./services/blogpost-service.js').BlogpostService}} Modules
 * @typedef {Fastify & Modules} App
 */

export {};
